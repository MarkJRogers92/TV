# MarkTV Autopilot discovery — 2026-09-23

This is the Stage 0 baseline for the September 23 Autopilot package. It records observed state, not acceptance of the proposed policy. No live service was restarted during this inventory.

## Running stack and source

- `com.marktv.server` runs `/usr/local/bin/node dist-server/src/server/index.js` in this checkout on port 4177. Process 92223 started September 23 at 19:04 local. Branch `fix/watch-live-playback` at `3efd39f`; the checkout was clean when inspected. The built `dist-server/src/server/index.js` SHA-256 was `01083d3222969001e1ab89896122299b3e128791603305e1c5bd64faf42f9f15`. A detached clean build of `3efd39f` under `/Users/markrogers/marktv-ops/autopilot-baseline-build` produced an identical `dist-server` tree, including that SHA-256. The separate `/Users/markrogers/TV` checkout has a different built hash and is not the running source.
- `com.marktv.tunarr-patched` runs `/Users/markrogers/tunarr-node22/bin/node /Users/markrogers/tunarr-fork/server/dist/bundle.cjs` on port 8000. Process 30599 started September 23 at 04:06 local. Its bundle SHA-256 was `b0e70dc5a4bc8ad9d93efc82d4ec6f5f39e4e41ce47d839816de9c2d2c9458fd`. `/api/version` reports Tunarr 1.3.15, FFmpeg 9.0.1, Node 22.20.0. The Tunarr branch is `codex/marktv-playback-stability` at `a926bd6e`; `pnpm-workspace.yaml` and `server/src/generated/env.ts` have uncommitted build-related edits. The running bundle/source equality is not yet proven.
- Tunarr uses `/opt/homebrew/bin/ffmpeg` and `/opt/homebrew/bin/ffprobe`, both 9.0.1. Current observed FFmpeg command produces 854x480 H.264/AAC HLS with four-second MPEG-TS segments, `-readrate 1` when paced, and the existing channel watermark. Source contains bounded `suppressInputThrottle` and `realtime` producer policies; these are preservation constraints.

## Channels and media

Tunarr reports channel 7 MarkTV Laughs (`38fec30b-1534-4520-9a51-2e39c925cddc`), channel 8 MarkTV Movies (`95c24cf1-4c7e-42e8-8814-8bbd5173f0b8`), and channel 9 MarkTV Cult Movies (`3d3861ae-b913-4848-9512-1180e631732a`). All are HLS with on demand disabled and shared transcode config `3725b1ad-99bb-4072-ae37-58f02a8b26ac`. At inventory time channel 7 had one active TiviMate 5.3.3 client; channels 8 and 9 had no session. Do not use an HLS fetch as a passive monitor because it creates a client session.

The live media volume is ExFAT `/Volumes/SSK Drive ` (trailing space), UUID `C0AA695F-3F9D-38AB-8F96-CC34B2EE9968`, with 794 GiB available at inventory. Launch settings point MarkTV at `MarkTV/Inbox` and `MarkTV/Shows`; media roots also include `MarkTV/Commercials`, `MarkTV/Movies`, and nested `MarkTV/Movies/Cult`. The MarkTV database catalogs 1,605 media records, including 377 episodes, 48 movies, and 928 commercials; pools include 29 main movies and 19 Cult movies. Filesystem file counts include other formats and nested files and are not interchangeable with playable catalog counts. The internal disk had 69 GiB available. The `filler` pool was empty; no verified internal-drive emergency pool was found.

## State and present mechanisms

- MarkTV's authoritative database is `data/marktv.sqlite` in this checkout (SQLite delete journal mode). Tunarr's database is `~/Library/Preferences/tunarr/db.db` (WAL mode). The existing movie rotation and dated occurrence tables hold 16 assignments for MarkTV Laughs from September 20 through October 1; preserve them. The MarkTV database also contains three channels, 19 pools, 816 settings and schedule generations.
- The current 16 movie assignments include distinct ordinary nightly titles through October 1 and explicit September 27/28 encore links to the preceding weekend openers. They are reservations, not verified screenings. The September 20 table contains one opener row, so do not infer a complete weekend block from occurrence count alone.
- `src/server/scheduleRefresh.ts` performs single-flight periodic day generation and guarded sync. `src/server/scheduleService.ts`, `src/scheduler/generate.ts`, `src/scheduler/select.ts`, and `src/db/repositories.ts` own lineup generation, selection, and history. `src/scheduler/movieProgramming.ts` and `src/domain/movieProgramming.ts` already implement persisted movie bags, dated assignments, weekend encore, carried movies and break placement. Continuity Director is in `src/continuity/`. Existing Tunarr sync lives under `src/integrations/tunarr/`.
- Episode history currently comes from the newest generated schedule per past date, which is planned content rather than proven airing. `chooseChronological` wraps through the ordered list. There is no established on-air completion ledger for episodes. Treat migration and replay prevention as high risk and do not infer that all historical scheduled entries completed.
- Tunarr has 3,986 rows in `program_play_history`, but `StreamProgramCalculator.ts` inserts one when a content-backed lineup item is calculated. This is request/selection evidence, not proof that its output was published and aired for its full duration. It cannot be imported as a completed-episode ledger without stronger correlation.
- Existing `com.marktv.playback-health`, `com.marktv.tunarr-watchdog`, and one-shot midnight movie sync launch agents/scripts exist, though only server and Tunarr appeared in `launchctl list` at inventory. Their activation and policy must be inspected before replacing or duplicating supervision.
- The one-shot `com.marktv.midnight-movie-sync-once` agent is loaded for September 24. Its script targets channel 7, verifies a date-specific movie schedule, and can restart the patched Tunarr service once if an active session blocks the midnight sync. It retires itself after success/window expiry. Treat it as a scheduled live operation when choosing a deployment window; do not silently replace it with another restart loop.
- Built in pieces: media scan and import, movie rotation and partial lookahead, exact break filling, continuity assets, guarded sync, existing HLS pacing and browser recovery. Partial or missing for this package: actual-airing episode state, 72-hour committed rolling reservations, observed movie exposure and stronger nightly spacing, internal emergency pool, graded decode/preparation queue, channel-scoped recovery evidence, unified health view, and 24–48 hour client soak. These classifications require stage-specific validation.

## Stage 0 backup and evidence limits

Before code changes, consistent SQLite backups, the running server entry, Tunarr bundle, launch plists, settings, dirty patches and SHA-256 manifest were saved under `/Users/markrogers/marktv-ops/backups/autopilot-20260923-2225`. Both SQLite backup copies passed `PRAGMA integrity_check`. This backup is a baseline only: a later rollback must preserve and reconcile newer playback history rather than restore these database files over live state.

At inventory, channel 7 was actively producing HLS files and an FFmpeg process was encoding a Night Court episode. This shows a live producer, not a completed episode/ad/movie transition or client picture/audio acceptance. No actual TiviMate screen, Safari playback, web HLS playback, or extended soak was observed in Stage 0.
