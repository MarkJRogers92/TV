# Autopilot implementation checkpoint — September 23, 2026

**Scope:** Work started from the September 23 handoff in `/Users/markrogers/Downloads`. This is an implementation checkpoint, not a claim that the full package is live.

## Implemented in source

- Stage 0 discovery and consistent live database/bundle backups: `MARKTV_DISCOVERY.md` and `/Users/markrogers/marktv-ops/backups/autopilot-20260923-2225`.
- Stage 1 partial: chronological episode selection no longer cycles back to the first episode or skips a missing ordered successor based on the supplied history. A second encode with the same season/episode position cannot be selected as the next episode. Cooldown relaxation does not bypass the no-replay rule for episodes.
- An additive shadow-only airing ledger now records shared series tracks, episode identities, reservations, attempts, published and aired source intervals, an active occurrence/source offset, holds, and monotonic completion floors. It refuses duplicate ID conflicts, source-offset regressions, competing active occurrences, missing numeric successors, and completion without contiguous published and aired coverage. An explicit initial position exists for future migration, but no migration or real evidence feed has been performed. The selector is not wired to this ledger and is not active in the live build.
- Continuity guard: background refresh isolates one channel's generation/sync failure and skips disabled channels. This does not yet supply a 72-hour committed horizon or emergency media.
- State-safe rollback procedure recorded in `AUTOPILOT-ROLLBACK.md`; a copied-database schema compatibility rehearsal preserved a synthetic newer active occurrence and 600,000 ms source offset when reopened by the preserved baseline database code. An actual live binary rollback and history reconciliation remain untested.
- The approved technical-difficulties, more-television-shortly, and station-ID clips were copied without changing their originals to `~/Library/Application Support/MarkTV/emergency-assets/v1-20260923`. The manifest records matching source/destination SHA-256 hashes and `activated:false`; FFprobe and full audio/video decode succeeded. Fallback selection has not been wired.
- A passive multi-channel recorder now runs every 60 seconds from `com.marktv.autopilot-soak`. Its version-controlled source and parser tests are in `scripts/ops/`; its deployed copy, logs, and runbook are under `/Users/markrogers/marktv-ops/`. It reads existing Tunarr sessions and local raw HLS output without tuning a channel or restarting playback.
- A shadow-only nightly movie spacing function accepts explicit verified exposure, counts an aired overnight encore as exposure without another rotation draw, protects a 21-day ordinary-night floor, and chooses a 30-day gap where feasible. Existing production callers pass no exposure, so their draw remains the baseline one until the airing bridge is active.

## Verification completed

- `./node_modules/.bin/vitest run tests/scheduler/select.test.ts tests/scheduler/seriesRotation.test.ts tests/scheduler/generate.test.ts tests/server/scheduleRefresh.test.ts`: 47 tests passed in four files. After independent review exposed an unknown-metadata gap, the selector was corrected and `./node_modules/.bin/vitest run tests/scheduler/select.test.ts tests/server/scheduleRefresh.test.ts` passed 37 tests in two files.
- `./node_modules/.bin/tsc --noEmit`: passed.
- `git diff --check`: passed.
- `./node_modules/.bin/vitest run tests/scheduler tests/server/scheduleRefresh.test.ts tests/db/repositories.test.ts`: 140 tests passed in 13 files after updating the episode midroll fixture to use a distinct second episode. The old one-episode fixture expected an automatic replay at 00:30, which conflicts with the new no-replay rule; the updated test still checks alignment and now checks that the next episode is actually different.
- Both Stage 0 SQLite backup copies returned `ok` from `PRAGMA integrity_check`.
- `node --test scripts/ops/autopilot-soak.test.mjs`: three parser tests passed; the deployed recorder has exited successfully on scheduled runs. Its latest baseline observation saw channel 7 producing with no recent raw-playlist timestamp gaps or missing recent segment files, and channels 8/9 idle without viewers. These are producer observations, not client picture/audio acceptance.
- `./node_modules/.bin/vitest run tests/autopilot tests/db tests/scheduler tests/server/scheduleRefresh.test.ts`: 167 tests passed in 16 files after the ledger correction, package F02 fixture adapter, and active-offset boundary test. `./node_modules/.bin/tsc --noEmit`, targeted ESLint, and `git diff --check` passed. The supplied `04_TEST_FIXTURES.json` was copied intact under `tests/fixtures`; F02 drives an actual selector test from its data.
- After integrating the corrected shadow movie policy and package F01/F03 adapters, the combined relevant Vitest run passed 190 tests in 17 files, TypeScript and diff checks passed. Independent review found and then cleared a future-exposure defect with a per-date filter and regression test. F01 verifies durable active offset and reservations after reopening SQLite; F03 verifies future publication alone cannot complete or advance the ledger.
- At 23:04 CDT on September 23, the live baseline Watch Live page in Safari initially displayed Paused; a manual Play action changed it to Live and a visible channel 7 sitcom picture appeared. Tunarr reported two connections on channel 7, including the pre-existing TiviMate session. The playback clock advanced, but around 23:11 Safari showed repeated Buffering states, changing hls.js blob URLs and a black video frame for more than one observation, then briefly returned to Live around 23:13. Safari Web Inspector later showed `XMLHttpRequest cannot load ... due to access control checks` for older `data000855.ts`–`data000870.ts` segment requests; a subsequent Network capture showed manifests and newer `data000954.ts`–`data000961.ts` returning 200. Raw local HLS still had recent contiguous segments. The initial cause and precise failed response remain unproven; audio, the TiviMate screen, and a program boundary remain unverified. This is a baseline client failure that must be diagnosed before release; it does not validate the unactivated ledger or continuity soak.
- A second Safari hls.js baseline session was manually started around 23:32 CDT. Visible channel 7 episode frames and the Safari playback clock advanced for more than nine minutes. Around 23:39 the picture changed to a visually distinct vintage commercial clip and by around 23:41 returned to the episode, with the player still reporting Live. This is one observed episode-to-break-to-episode picture transition in Safari, not a complete episode, audio verification, native Safari HLS, or TiviMate screen result. The earlier buffering failure remains unexplained.
- The passive recorder was updated at 23:38 to report recent discontinuity deltas separately from unmarked timeline gaps. The deployed and committed script hashes match (`6c3ce7495614d1622dcf676b3ce33d56148932de28f5d49df4dbc6fd3fb54064`); its three parser tests and one manual scheduled-format run passed. The first new record observed 106 total channel 7 discontinuities and recent deltas around -0.35 seconds while the channel still produced. Earlier observed deltas around a discontinuity were roughly +/-2 seconds, so raw PDT alone is not exact source coverage.
- At 00:01 CDT on September 24, the pre-existing one-shot midnight sync logged one Tunarr restart, applied and verified channel 7 schedule `marktv-laughs-2026-09-24-30374de0`, then retired its own LaunchAgent/script. Tunarr returned as v1.3.15 and channel 7 resumed producing. Safari's pre-existing Watch Live tab displayed channel 7 picture and `Live` after that restart. Switching the same Safari hls.js player to channels 8 and 9 showed initial Connecting/Buffering followed by visible movie picture, `Live`, and advancing elapsed time on each. This is a short baseline picture check across all three channels, not audio, TiviMate screen, native Safari HLS, long transitions, or Autopilot canary proof.
- Additional supplied fixture adapters F17/F18 pass in isolated branch `codex/autopilot-fixtures-next`; F06 is a deliberately failing regression there. The local scanner keys media by full path, so renaming a movie and adding a normalized rendition gives it a new ID that bypasses raw-media-ID exposure history. Parent reproduced the F06 failure (7 other package tests passed); this branch has not been integrated.
- Two independent evidence prototypes remain isolated. The Tunarr pure receipt helper passed 11 isolated tests and TypeScript, but review found that output `EXTINF` duration alone cannot establish source identity/offset; sliding playlists and HLS GAP tags are also unhandled. The MarkTV shadow applied-lineup binding prototype passed 10 actual Vitest tests and TypeScript, but independent review found that matching a lineup by slot shape does not prove its Tunarr program ID belongs to the scheduled MarkTV media. Neither prototype is wired to live playback or credited to the ledger.
- A post-midnight-sync online backup of both live SQLite databases was saved at `/Users/markrogers/marktv-ops/backups/autopilot-20260924-0011-post-sync`; both copies returned `ok` from `PRAGMA integrity_check`. The backup README records hashes and requires keeping newer live playback history during any binary rollback.

## Gates and runtime status

- Stage 0 is **partial**: a clean rebuild of MarkTV commit `3efd39f` matched the complete running `dist-server` tree. An isolated rebuild of Tunarr commit `a926bd6e` with the preserved local version/build env produced a `bundle.cjs` byte-identical to the running bundle (SHA-256 `b0e70dc5a4bc8ad9d93efc82d4ec6f5f39e4e41ce47d839816de9c2d2c9458fd`); no live Tunarr file or service changed. Full `dist` trees differ in generated metadata and duplicate copied asset directories, so this proves the application bundle, not every runtime file. An actual client transition on each channel remains unproven. A channel 7 TiviMate session and active HLS producer were observed via Tunarr API/process/filesystem; the Safari observation below found intermittent black buffering.
- Stage 1 is **not passed**: the existing `historyBefore` repository reads generated schedules as history, and Tunarr's `program_play_history` records lineup calculation. Neither proves completed airing. MarkTV schedule entry identity is dropped by its current Tunarr sync, so a verified occurrence/source-offset bridge is still needed to feed the new shadow ledger. Activating the selector alone could omit episodes that were planned but never aired. No database migration or live schedule replacement has been performed.
- Stages 2–6 are **not passed**. The shadow movie spacing policy is source-only and has no verified live exposure feed. Existing movie reservations, weekend encore and Tunarr pacing/buffer changes remain the live baseline. No background preparation queue, activated internal emergency pool, channel-scoped recovery, all-channel canary, real-client acceptance, or 24–48 hour post-canary soak is claimed.
- The new source changes have **not** been built into or deployed over the running MarkTV/Tunarr services. No live service restart or stream interruption has occurred during this checkpoint. User authorization to interrupt streams for a gated deployment is recorded, but it is not a substitute for the Stage 0/1 evidence.

## Next critical work

Build a verified publication/airing evidence bridge and migrate a monotonic per-series floor using evidence stronger than scheduled rows; preserve the active source offset and reserve future episodes separately. Then exercise duplicate callbacks, restart/rename, missing successor, and failure recovery on copied state before any live selector activation. Integrate the 72-hour future plan and stronger movie exposure policy after that gate. The complete background automation suite and actual client/soak checks remain in scope.
