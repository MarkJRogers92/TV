import { render, screen, within } from '@testing-library/react'; import { expect,test } from 'vitest'; import { App } from '../../web/App';
test('renders the MarkTV application shell',()=>{render(<App/>);expect(screen.getByRole('heading',{name:'MarkTV'})).toBeVisible();const navigation=screen.getByRole('navigation');expect(navigation).toBeVisible();expect(within(navigation).getByRole('link',{name:'Watch Live'})).toBeVisible();expect(screen.getByRole('link',{name:'Wanted'})).toBeVisible();expect(screen.getByRole('link',{name:'Integrations'})).toBeVisible();});
